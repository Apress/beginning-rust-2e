/* The compiler prints the two warning messages:
unused comparison that must be used
unused arithmetic operation that must be used
Then, the program prints nothing.
*/
fn main() {
    let a = 7.2;
    12;
    true;
    4 > 7;
    5.7 + 5. * a;
}
